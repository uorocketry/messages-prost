syntax = "proto3";

package messages_prost.argus;

message Pressure {
  double pressure = 1;
  uint32 sensor_id = 2; 
}

message Strain {
  double strain = 1; 
  uint32 sensor_id =2; 
}

message Temperature {
  double temperature = 1;
  uint32 sensor_id = 2; 
}