syntax = "proto3";

package messages_prost.argus_state;

enum ArgusState {
  INIT = 0;
  CALIBRATION = 1; 
  IDLE = 2; 
  COLLECTION = 3; 
}

enum ArgusEvent {
  REQUESTED_CALIBRATION = 0; 
  CALIBRATED = 1; 
  WANTS_COLLECTION = 2; 
}